

void main() {
//final greeating = greet(85,05); // if I write like this it will be work  but 


//greet ( String name ,int  age){ 
  
  // if I invoked String and int it will take only this type of variables
 //  return("My names $name , and I am $age");
//}
final greeti = greet(age:53,name:"vek"); //here I wanna show it doesn't matter which variables will stay first in functions couse I invoked name paremeters 
 String ({ required names,int? ages, required numbersaaaa}){ //here we go If I invoked variables and add all of them required means it must be fill ? means not essential and we use with {}
   return"My name is ";  // in here i wrote code must be return type of String  and I f I delate como it gonna errow
}